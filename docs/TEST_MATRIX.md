# Test Matrix

This file maps Planora product behavior to proof. It reflects the **actual**
state of the test suite, not aspiration. Do not mark a row `implemented` until
tests or validation evidence exist.

Unit/integration runner: **Vitest 2** (node env). Commands: `npm test` (run
once), `npm run test:watch`. Two Vitest projects (`vitest.workspace.ts`): `node`
(`lib/**/*.test.ts` + `tests/**/*.test.ts`, node env) and `components`
(`components/**/*.test.tsx` + `app/**/*.test.tsx`, happy-dom env, added US-068).
E2E runner: **Playwright** (`npm run test:e2e`, `e2e/**`), added in US-009 — a
two-client realtime harness, chromium. **CI** runs the unit/integration gate
(`ci.yml`, US-008, required-eligible) and the E2E suite (`e2e.yml`, US-009,
separate non-blocking). React Testing Library now exists (US-068) with a
representative set of client-component tests; most React component internals are
still unverified (thin coverage, not zero). Async Server Components stay covered
by E2E, not RTL.

**2026-08-08 E2E baseline repair (`fix/e2e-today-ci`):** two test-side defects,
no product change. (1) `demo-rehearsal.spec.ts` toast assertion filtered to the
quick-capture `role="status"` toast (`filter({ hasText: "Card created" })`) —
`/today` now also renders the polite end-of-list status, making the bare
`getByRole("status")` strict-mode ambiguous. (2) `today.spec.ts` AC5 arrange
steps now wait for the archive Server Action to commit (DB poll in
`archiveCard` + `getBoardArchivedAt`) before navigating to `/today` — the
immediate goto raced the transaction, so the archived card/board leaked into
Due Today and the count badges read 3/2 instead of 2/1. All card/link/count
assertions preserved verbatim.

## Status Values

| Status | Meaning |
| --- | --- |
| planned | Accepted as intended behavior, not implemented |
| in_progress | Actively being built |
| implemented | Implemented and proof exists |
| changed | Contract changed after earlier implementation |
| retired | No longer part of the product contract |

## Matrix

| Contract | Unit | Integration | E2E | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Card history events (created/moved/completed/reopened/estimate) | yes | yes | no | implemented | `lib/card-history.test.ts` (11 cases). `buildCardReopenedEvent`/due-date/member/archive builders + read paths still untested. |
| Board store: remote apply + drag-defer + self-echo dedupe (0008) | yes | partial | no | implemented | `tests/board-store.test.ts` (54 cases) + `app/(authenticated)/(dashboard)/boards/[boardId]/board-store-provider.test.tsx` (3, provider wiring) |
| Board live presence registry (US-041, decision 0012) | yes | no | yes (E2E) | implemented | `lib/realtime/presence.test.ts` (9 cases): per-user dedupe across tabs, last-tab-leaves change detection, `removeSocket` across multiple boards (and no-op when another tab remains), leave→disconnect idempotency, reconnect interleave (new socket before old removed keeps the watcher), stable name sort. Full wire proven end-to-end by `e2e/realtime-presence.spec.ts` (two real browser users on one board): both see two presence avatars when both are viewing, and the count drops to one live when one user leaves — exercises the real `server.ts` `board:join`/`disconnect` → `emitBoardPresence` broadcast, client `applyRemotePresence`/`seedWatchers`, and the header avatar group. Note: `server.ts` is not hot-reloaded by `tsx`, so presence wiring requires a dev-server restart to take effect. |
| DnD position math + reference preservation (`translate*Drop`) | yes | n/a | no | implemented | `lib/dnd/apply-drop.test.ts` (15 cases) |
| Analytics engine (burndown, lead time, overdue, reopen, coverage) | no | yes | no | implemented | `lib/analytics/engine.test.ts` (13 cases) — incl. US-062 MJ2 (lead-time rows capped at the newest completions, consistent with `totalCompleted`) and mn3 (an unparseable due-date string is treated as no due date, not silently dropped). |
| Analytics CSV export + escaping (formula-injection guard) | yes | yes | no | implemented | `tests/analytics-export.test.ts` (7 cases) + `lib/csv.test.ts` (8, pure-formatter cell-level escaping): comma/quote escaping, formula-leading `=`/`+`/`-`/`@`/tab/CR guarded, embedded newline quoted, US-062 MJ1 `Board ID`/`Member ID` header cells guarded. The formatter moved to the pure, client-safe `lib/analytics/csv-export.ts` (US-062 mn17 — no longer a `"use server"` export). |
| Server Action security boundary: auth + permission gate + workspace isolation on every mutation (US-006) | yes | partial | no | implemented | `tests/server-actions/{board,list-card,workspace,analytics-read}.test.ts` — 166 cases (154 pre-P0 + 8 added by PR #98 on `board` + 4 added by PR #99 on `list-card`; vitest-verified 21+120+19+6) (A1 auth / A2 permission / A3 isolation + positive control) over 32 mutating + 2 read actions, incl. `moveCardAction` cross-workspace rejection. Sabotage-verified: removing a gate turns A2/A3 red. `toggleBoardStarAction`/`createWorkspaceAction` now have A1/A2/A3/positive-control coverage (member-any gate; createWorkspace is session+Zod only — no workspace to scope). **US-062 tg2:** the `list-card` positive controls for `createCard`/`deleteList`/`moveCard`/`assign`+`removeCardMember` now run the real `db.$transaction` callback against a fake tx and assert the DB effect (card inserted at a gap position, list deleted, card relocated to the target list at a resolved position, member row created/deleted, history captured) — not merely that `$transaction` was called. |
| Realtime socket room-authorization boundary (US-062 tg1, decision 0017) | yes | n/a | via US-009 | implemented | `lib/realtime/auth.test.ts` — 19 cases. `getBoardMembershipRole` denies missing/archived/foreign board + non-member, scopes the membership lookup to the board's workspace + user, returns known roles verbatim, normalizes unknown roles to `viewer` (least privilege), fails closed on db error; `canUserJoinWorkspace` allow/deny/fail-closed; `authenticateSocket` cookie handling (case-insensitive) + fail-closed; `getUserProfile`. This is the realtime analogue of the RBAC matrix — a regression here would leak a workspace's live board stream to a non-member. Full wire is exercised by the US-009 two-client E2E. |
| RBAC role × action allow/deny matrix (US-007) | yes | n/a | no | implemented | `tests/server-actions/rbac-matrix.test.ts` — 145 cases (the `canPermanentDelete` cell was added by commit 7825c17; +3). L1: every role × (entity,verb) cell of the real `admin`/`editor`/`viewer` roles (`lib/permissions.ts`) + AND semantics. L2: board-page UI map (`getBoardPagePermissionsForRole`) agrees with the server matrix. L3: US-006 `roleGrants` copy matches the real matrix. Layer-checked against the real roles. |
| Board CRUD (create/update/delete/archive) | no | partial | no | planned | Security boundary proven (US-006, `board.test.ts`); business logic still unit-untested. |
| List CRUD + reorder + isDone toggle | yes (reorder) | partial | no | implemented (reorder) | Security boundary proven (US-006, `list-card.test.ts`). Reorder position math now unit-tested: `lib/list.test.ts` (US-062 MJ3) covers `resolveListPosition` — live-adjacent re-query (concurrent end-drop bisects against the real follower), before-first, empty append, exclude-mover, and StaleNeighborError; plus a `reorderListByNeighbors` stale-neighbour recovery (drop hint → append). List lifecycle paths (archive/restore/get) unit-tested in `lib/list.test.ts` (US-074 resolver coverage). |
| Card CRUD / move / archive / details / estimate / due date | yes (reorder) | partial | no | implemented (reorder) | Security boundary proven (US-006, `list-card.test.ts`); `moveCard`/`createCard` transaction bodies now exercised (US-062 tg2). Reorder position math unit-tested in `lib/ordering.test.ts` + `lib/card.test.ts` (US-062 mn2: `resolveCardPosition` throws a retryable `StaleNeighborError` on a stale/foreign hint; `reorderCardWithinListByNeighbors` drops the hint and appends). Card completion + resolver hardening in `lib/card.test.ts` (US-045 completion compare-and-set; archived-parent resolver discrimination); remaining lifecycle math thin. |
| Card members assign/remove | no | partial | no | planned | Security boundary proven (US-006, `list-card.test.ts`); assign/remove tx bodies exercised (US-062 tg2). |
| Card labels: schema + data layer (CRUD, attach dedupe, detach) | yes | partial | no | implemented | `lib/schemas/label.test.ts` (8), `lib/label.test.ts` (6). Action permission gating (US-005) not yet integration-tested. |
| Card labels: board-store `card:labels-updated` (apply / ref-preserve / self-echo dedupe / scope) | yes | partial | manual | implemented | `tests/board-store.test.ts` (6 label cases). Card-face chips + attach/detach realtime verified manually in browser (US-005 slice 2). |
| In-board card filtering by label (US-013) | yes | n/a | manual | implemented | `lib/board-filter.test.ts` (9 cases: `cardMatchesFilter` empty/OR/no-match, `isFilterActive`, `availableLabels` dedupe+sort+first-seen+empty). Client-side view filter — no DB/Server Action. Manual browser QA (Chrome DevTools): control hidden with no labels, appears once a label is in use, selecting a label hides non-matching cards + shows count badge, Clear restores. Non-matching cards hidden via CSS (Draggable stays mounted) so drop indices stay aligned. Assignee/due-date filtering deferred (slice 2 — needs card payload enrichment). |
| In-board card search by title (US-014) | yes | n/a | manual | implemented | `lib/board-filter.test.ts` (+5 cases: `isSearchActive`, `cardMatchesQuery` empty/whitespace, case-insensitive substring, no-match) → 14 total. Client-side view filter — no DB/Server Action. Composes with the US-013 label filter via AND. Manual browser QA (Chrome DevTools): live title narrowing, case-insensitive match, no-match shows "No cards match" hint, clear (✕) restores, search+label-filter AND leaves only the card matching both. Non-matching cards hidden via CSS (Draggable stays mounted) so drop indices stay aligned. Title-only in slice 1 — description/assignee/due-date search deferred (needs card payload enrichment). |
| Comments | no | partial | no | planned | Security boundary proven (US-006, `list-card.test.ts`: viewer may comment, non-member cannot); `lib/comment.ts` business logic untested. |
| Checklists — CRUD Server Actions + security boundary (US-015) | no | yes | manual | implemented | `tests/server-actions/checklist.test.ts` — 23 cases (5 actions × 4 + 3 guard tests). A1 auth / A2 viewer-denied / A3 cross-workspace isolation + positive control for each of the 5 actions (create/delete checklist, add/toggle/delete item), all gated by `card:["update"]` with workspaceId derived from the checklist's board; plus archived-board/archived-card guards. Sabotage-verified (defeating one gate turns its A2+A3 red). `lib/checklist.ts` is thin (Prisma + float-gap append). Detail-sheet UI (`card-checklists-section.tsx`) manually QA'd in browser (Chrome DevTools): create/add-item/toggle/delete-item/delete-checklist all persist via the real action→Prisma→revalidate path; checklist create/delete log a `CHECKLIST` activity entry, item toggles don't. Automated E2E deferred (same board-UI debt as US-005/013/014). Rename/reorder/realtime deferred. |
| Card archive/restore — restore Server Action + security boundary (US-016) | no | yes | manual | implemented | `tests/server-actions/list-card.test.ts` — `restoreCardAction` (+6 cases): A1 auth / A2 viewer-denied / A3 cross-workspace isolation + positive control, plus archived-board guard and not-archived/foreign-id guard (`getArchivedCardWithListAndBoard` returns null). Gated by `card:["delete"]` (same as archive); workspaceId derived from the card's board; an **archived-aware** resolver finds the card the default `getCardWithListAndBoard` filters out. Records `CARD_RESTORED` history and re-emits `card:created` so the card reappears live. Sabotage-verified (defeating the gate turns A2+A3 red). **US-083 W8 (decision 0031):** the resolver now FLAGS the archived-parent case (`parentListArchived`) instead of nulling it, and `restoreCardAction` (a) surfaces the dedicated "Restore the list first." outcome (`code: PARENT_LIST_ARCHIVED`) only after the card-exists/remains-archived/parent-list-archived/board-active/authorized predicate holds — no existence leak (viewer/foreign/archived-board/missing keep generic not-found) — and (b) re-checks the parent list INSIDE the transaction under `SELECT ... FOR UPDATE` + `archivedAt IS NULL` revalidation (US-074 pattern), aborting the restore if a concurrent list archival committed between the pre-read and the transaction. Proof: `tests/server-actions/undo-restore.test.ts` (13 — no-leak viewer/foreign discriminations, sequential + true-race dedicated outcomes, list-row-gone generic failure, double-undo residual generic contract, FOR UPDATE call-shape, CARD_RESTORED + emits, restoreListAction path; the race case goes RED when the in-tx revalidation branch is removed — sabotage observed); `tests/db-undo-race-proof.test.ts` (real-Postgres lock_timeout-deterministic interleaving: guarded protocol aborts the archiver-commits-first interleaving, unguarded control commits the invisible card — the harness detects the violation, FOR UPDATE lock blocks the archiver then archive-after-restore proceeds; guard-removal sabotage turns the invariant test RED); `lib/card.test.ts` resolver discrimination (2). Full-suite checkpoint at US-016 close: 411 green; current full suite: `npm test` 1404 green / 90 files (US-083 final close). Archived-cards view (`archived-cards-dialog.tsx`, board header, editor/admin only) manually QA'd in browser (Chrome DevTools): empty state, archive Card A → appears "in To Do" → Restore → reappears on board with its label intact, no console errors. Stale "cannot be undone" archive-confirm copy corrected. List/board restore + permanent delete deferred. |
| Depth by surface ladder + hairline, not drop shadow (US-048, IN-03) | no | no | manual | implemented | Presentation/token conformance to `DESIGN.md` §95/§273. No automated coverage (no RTL); proof is manual browser QA (Chrome DevTools, light + dark, DOM-verified `getComputedStyle`): resting cards/tiles/inputs `boxShadow: none` + 1px `border-border` (dark = white@10%) + `rounded-lg` (7.2px); dragged card lifts via `shadow-md` (soft 0.1-alpha) + `scale: 1.02`; floating modal keeps its shadow; no console errors. shadcn primitives (`card`/`button`/`input`/`textarea`/`checkbox`) annotated `// customized:` to survive re-sync. See US-048 story Evidence. |
| Semantic success/warning status tokens, no ad-hoc green/red/amber (US-050, IN-03) | no | no | manual | implemented | New `--success`/`--success-foreground` + `--warning`/`--warning-foreground` token pairs in both themes (`app/globals.css`), `@theme inline` aliases, measured-AA comments (light: success-fg 5.54:1 / warning-fg 5.65:1 vs card; dark: 10.07:1 / 10.91:1). All ad-hoc Tailwind `green/red/amber/emerald` migrated to tokens (or `destructive` for late/error) across the six analytics surfaces `kpi-cards`, `burndown-chart`, `flow-chart`, `lead-time-table`, `data-quality-section`, `launch-boundary-banner`; `list-card-item.tsx` due/priority chips and `board-header.tsx` badge retain ad-hoc colors (documented as remaining). Status color always reinforces a non-color signal (arrow/sign/icon/label — WCAG 1.4.1). No automated coverage (no RTL); proof is manual browser QA (Chrome DevTools, light + dark, DOM-verified `getComputedStyle` + compiled-CSS token check): every status surface resolves through the tokens in both themes, no console errors. Decision 0013 (Accepted). See US-050 story Evidence. |
| Analytics chart series resolve from `--chart-*` / token, no inline hex (US-049, IN-03) | no | no | manual | implemented | Retired the 3 hard-coded chart hex literals: burndown line `#3b82f6` → `var(--chart-2)`, flow "Created" `#6366f1` → `var(--chart-2)`, flow "Completed" `#10b981` → `var(--success-foreground)` (US-050's deep green). Burndown uses `--chart-2` not the specced `--chart-1` — `--chart-1` is the palest ramp rung at 1.81:1 on the white card (fails WCAG 3:1 for graphical objects); `--chart-2` clears it (3.76:1 light / 4.73:1 dark) and matches the prior hex. No automated coverage (hand-rolled `<svg>`, no RTL); proof is manual browser QA (Chrome DevTools, light + dark, DOM-verified `getComputedStyle` on rendered SVG strokes + legend swatches): both flow series categorically distinct (blue vs green) and resolve through tokens in both themes, completed-green swaps to the brighter dark value, no console errors. Burndown empty-state in QA data; `--chart-2` legibility proven by the flow "Created" line (same token/mechanism). See US-049 story Evidence. |
| Label chips tokenized to per-hue tint + deeper same-hue text (US-051, IN-03) | no | no | manual | implemented | Retired the `text-white`-on-raw-fill AA defect across the label surfaces. Each of the 8 `BOARD_COLORS` hues maps to a `--label-<hue>` / `--label-<hue>-fg` tint+foreground token pair in both themes (32 measured values, `app/globals.css` + `@theme inline` aliases, decision 0014), with the hue angle derived from the stored hex; mapped once via `lib/label-colors.ts` (`labelHue`/`labelChipStyle`/`labelSwatchStyle`, gray fallback for legacy colors). `label-mark.tsx` chip + `card-labels-section.tsx` attached chips render tint+deeper text (`rounded-sm`, no `text-white`); list/filter swatches (`card-labels-section.tsx`, `board-filter.tsx`) render tint + deeper-hue border; `color-palette.tsx` picker hover is now tonal (`brightness`, §68) not opacity. Bar form keeps its colorblind texture; picker keeps the saturated palette identity. Measured AA (text vs tint, all ≥4.5:1): light 4.60–4.66, dark 7.11–7.44. No automated coverage (no RTL); proof is manual browser QA (Chrome DevTools, light + dark, DOM-verified `getComputedStyle` per hue): seeded legacy-hex labels resolve to the gray fallback, four labels recolored through the real picker to Red/Green/Orange/Teal each resolved to their exact pair in both themes, no console errors. Decision 0014 (Accepted). See US-051 story Evidence. |
| Card detail reads as a Notion document — structure pass (US-052, IN-03) | no | no | manual | implemented | Retired the two-column ~1120px layout with boxed sub-cards. `card-detail-sheet.tsx` is now a single ~720px reading column (modal `max-w-[min(96vw,768px)]`, content `px-8 py-8` = 32px) of `border-t border-border` hairline-divided sections — no `rounded-lg border bg-muted/*` section wrappers remain in the document body. The right `<aside>` (comments/activity) collapsed into the stack; the former priority/dates boxes + members + labels collapsed into a compact de-boxed property strip under the title (members/labels via popovers, priority/due/estimate inline) — no control removed, autosave (US-032)/date-picker (US-039)/mention/live-assignee (US-011) untouched. Sub-components de-boxed: `card-checklists-section.tsx`, `card-attachments.tsx`; `card-labels-section.tsx` refactored to an inline compact form. Title styled to the card-title spec (22px/500) via inline utilities (was `text-2xl font-semibold`; no `card-title` CSS token exists); description + comment body step to 16px/1.55 (`md:text-base` overrides the Textarea primitive's `md:text-sm`), meta stays 14px. Brand color only on the Post-comment action. No automated coverage (no RTL); proof is manual browser QA (Chrome DevTools, light + dark, desktop + 375px): DOM-verified description `fontSize 16px`/`lineHeight 24.8px` and meta label `14px`, 0px horizontal overflow at 375px, Labels/Members popovers operate in both themes, no console errors. See US-052 story Evidence. |
| Card detail lifecycle safety — autosave queue rejection recovery, meta-draft live reconciliation, same-tick single-flight, board rename draft preservation (fix/card-detail-lifecycle-safety, PR #119) | yes | no | no | implemented | `components/boards/card-detail-sheet.test.tsx` (+11 cases): queued-save rejection recovery — a thrown/rejected save surfaces the generic error (`Something went wrong. Please try again.`), the drain ownership resets (never stuck in "Saving…"), saves queued behind the rejected save still run in the same drain (controllable reject-then-continue), and later drains recover; meta-draft reconciliation — remote dueDate/priority/estimate changes (store `card:meta-updated` → `liveCard`) flow into the drafts when not interacting, an open priority picker is protected mid-interaction and resyncs on close-without-a-pick, and a pick made while open is never resynced away (real Radix Select open/select/close interactions); comment-composer same-tick single-flight — Enter + submit click in the same tick posts once, retry after failure works, and a thrown action surfaces a visible generic error with exactly one call and a successful retry after. `components/boards/create-board-modal.test.tsx` (4): same-tick double submit creates exactly one board, retry after a failed/thrown action works, success resets + closes. `components/boards/board-header-rename.test.tsx` (3): the typed rename draft survives a blur while the save is in flight (input disabled mid-flight) and a failed save (edit mode + draft kept, error shown); the empty-draft blur still closes the editor and restores the last title (nothing-to-save, unchanged). Full gate: `npm test` 1625 green / 100 files; `tsc --noEmit` clean; changed-file ESLint 0 errors; `npm run build` compiles; `git diff --check` clean. Deferred (not addressed in this PR): the stale-meta failure-revert closure race — a failed meta save (`queueSavePriority`/`queueSaveDueDate`/`queueSaveEstimate`) reverts the draft to the `card` value captured when the save was queued, which can lag a concurrent remote change to that field. |
| Card detail close/reopen lifecycle — no post-close flash, reopen preserved (fix/card-detail-close-flash) | yes | no | no | implemented | `components/boards/card-detail-sheet.test.tsx` close/reopen lifecycle suite (4 cases, happy-dom): the dialog's open state is now URL-authoritative (`urlCardId === card.id` in `isOpen`) instead of trusting the server `open` prop, which lags during `router.replace` and can be resurrected by a stale in-flight `router.refresh()` payload that REMOUNTS the sheet (page.tsx keys it by the server card id) with `open=true` and a reset `dismissedCardId` — the reported intermittent flash. The latch is also cleared once the URL drops the card so an intentional reopen of the same card isn't swallowed. Tests drive the exact page.tsx keyed sequence (`key={card?.id ?? "card-detail-sheet-closed"}`) with a controllable URL double: (1) stale RSC payload remounting the sheet after close must NOT reopen it (RED before fix — this is the flash); (2) intentional reopen after close still opens; (3) stale payload keeping the sheet mounted (no remount) must not block a later reopen of the same card (RED before fix — latch-clearing proof); (4) Escape closes through the same `handleClose` path (`router.replace` strips `?cardId`). Full sheet suite 39 green; full `npm test` 1625 green / 100 files; `tsc --noEmit` clean; changed-file ESLint 0 errors; `npm run build` compiles. No CSS/DESIGN.md/motion changes; shadcn `components/ui/dialog.tsx` untouched. |
| No atmospheric gradient / second chromatic accent in chrome (US-053, IN-03) | no | no | manual | implemented | Retired the workspace-badge `bg-gradient-to-br from-sky-600 to-blue-800` (a gradient *and* a second chromatic accent in chrome, DESIGN.md §191/§386–389). `components/boards/styles.ts` constant renamed `workspaceBadgeGradient` → `workspaceBadgeSurface = "bg-secondary text-secondary-foreground"`; redundant `text-white` dropped from all three consumers (`workspace-item`/`workspace-section`/`workspace-boards-view`). Neutral chosen over brand `bg-primary` because the badge repeats per workspace — a brand fill would make blue ambient chrome and break "brand blue used scarcely". Card-cover scrim (`card-detail-sheet.tsx` `bg-gradient-to-t from-background`) resolved as *kept* with an inline rationale: a legibility fade over an arbitrary user image, not chrome decoration. `bg-gradient` grep over app/+components/ now returns only that annotated scrim; per-board `boardTheme` gradients (inline `linear-gradient`, ratified US-036/US-042), `label-mark` colorblind patterns, and the burndown SVG fill are not chrome accents. No automated coverage (presentational); proof is manual browser QA (Chrome DevTools, light + dark): badge resolves to neutral `bg-secondary` (`backgroundImage: none`) with strong-contrast initials in both themes (light `lab(96.52)`/`lab(7.78)`, dark `lab(15.20)`/`lab(98.26)`), no console errors. See US-053 story Evidence. |
| shadcn primitive conformance — tonal hover, radius tokens, px sweep (US-054, IN-03) | no | no | manual | implemented | Default `button.tsx` and brand `badge.tsx` link hover changed from `bg-primary/80` (opacity fade) to a new `--primary-hover` token (a lighter same-hue step, DESIGN.md §68). Token raises L per theme — light `oklch(0.52→0.56)`, dark `oklch(0.54→0.565)` — both AA vs `primary-foreground` even on the transient hover (4.62:1 / 4.51:1); `@theme inline` maps `--color-primary-hover`. Radius literals → tokens: `checkbox.tsx` `rounded-[4px]`→`rounded-sm`; `button.tsx` size variants `rounded-[min(--radius-md,8/10px)]`→`rounded-md` (the `min()` guard never clamped since `--radius-md`≈5.76px). Arbitrary widths → spacing scale (pixel-identical): `dropdown-menu` `min-w-[96px]`→`min-w-24`; `card-detail-sheet` `max-w-[14rem/15rem/10rem]`→`max-w-56/60/40`; `list-column` `max-w-[20rem]`→`max-w-80`. Kept exceptions: `scroll-area` `rounded-[inherit]` (CSS keyword), `card-detail-sheet` `max-w-[min(96vw,768px)]`/`h-[min(90vh,820px)]` (US-052 composite), `list-column` `w-[80vw]` + `add-list-button` `max-w-[20rem]` (fluid phone-peek family). shadcn-CLI files annotated `// customized:`. No automated coverage (presentational/primitive); proof is manual browser QA (Chrome DevTools, light + dark, dev server clean-restarted for the new `@theme` token): token resolves lighter than base in both themes, compiled CSS emits `hover:bg-primary-hover{…var(--primary-hover)}`, default button renders `border-radius:5.76px`, no console errors. See US-054 story Evidence. |
| Unread-notification non-color signal — WCAG 1.4.1 (US-055, IN-03) | no | no | manual | implemented | Unread rows gained a non-color signal beyond the `bg-primary` dot: title `font-semibold` (vs read `font-normal`) — a grayscale-perceivable weight contrast — plus an `sr-only "Unread: "` prefix for AT; the dot is now `aria-hidden`. Applied to both `notification-dropdown.tsx` (AC surface) and `notifications-list-client.tsx` (identical-markup sibling, same defect). Bell (`notification-bell.tsx`) was already conformant (count badge + `aria-label`) — untouched. No automated coverage (presentational); proof is manual browser QA (Chrome DevTools, seeded 1 read + 1 unread): unread title `font-weight:600` + sr-only label + aria-hidden dot + opacity 1, read `font-weight:400` + opacity 0.6; light + dark screenshotted, weight contrast clear in grayscale; no console errors. See US-055 story Evidence. |
| Attachments (Cloudinary upload/cleanup) | no | partial | no | planned | Security boundary proven (US-006, `list-card.test.ts`: denied callers never reach upload/write); upload/cleanup logic untested. |
| Auth / session / RBAC (admin/editor/viewer) | yes | partial | no | implemented | Per-mutation gate proven (US-006); full role × action allow/deny matrix proven against the real roles (US-007, `rbac-matrix.test.ts`); `hasWorkspacePermission` unit-proven (`lib/authorization.test.ts`, 5) + redirect hygiene (`lib/redirect.test.ts`, 5). Public auth forms RTL-covered (23 across `sign-in` 3 / `sign-up` 5 / `verify-email` 4 / `forgot-password` 6 / `reset-password` 5). `lib/auth.ts` session/login plumbing still unit-untested. |
| Workspaces + invitations (email) | yes | partial | yes | implemented | Settings + invite security boundary proven (US-006, `workspace.test.ts`); email formatting/hygiene unit-proven (`lib/email.test.ts`, 13 — from-header composition, display-name styles, CRLF/angle-bracket injection guards, missing-key no-op); invite/workspace business logic otherwise untested. **US-083 W2 adds the live-arrival contract:** `inviteMemberAction` emits the typed `invitation:new` user-room event for an already-registered invitee (case-insensitive normalized-email resolution — BA lowercases user + invitation emails, verified at source; unregistered email → no signal, invite still succeeds; lookup/emit failure never fails the invite) — unit-proven in `workspace.test.ts` (3 new cases) + `lib/realtime/server.test.ts` (4: 3 room-scope cases — user-room-only, never board/workspace — + 1 silent-no-op when IO uninitialized) + `lib/invitation.test.ts` (3: `getPendingInvitationCount` normalize/filter). Header contract proven in `components/authenticated-header-actions.test.tsx` (4: `invitation:new` increments the badge with no fetch; connect resyncs both badge halves from one DB-backed action). Full wire proven by `e2e/invitation-live-badge.spec.ts` (three real users): Alice invites registered Bob → Bob's badge increments live (no reload/navigation/inbox-fetch in the assertion path, masking tripwire armed on both observers, dropdown-open ordering guard) → inbox shows the invitation → Carol (connected outsider) sees no badge change → Accept clears the badge and Bob becomes a real `workspaceMember` (Postgres-asserted). Sabotage-verified: emit disabled → RED at the Bob live-badge observer assertion (tripwire clean). |
| Workspace member management (US-063): remove / role-change / leave / revoke | yes | yes | planned | implemented | Security boundary + last-admin invariant proven. `tests/server-actions/members.test.ts` — 21 cases: `removeMemberAction`/`updateMemberRoleAction`/`cancelInvitationAction` deny signed-out (throws), non-admin (pre-`auth.api`), and cross-workspace target/invitation (isolation); `leaveWorkspaceAction` denies signed-out and non-members only (self-removal has no non-admin/cross-workspace denial) and reselects a remaining workspace; positive controls assert the correct `auth.api.*` call (by `memberId`/`memberIdOrEmail`/`invitationId`), never a raw db write. R2 last-admin: removing/demoting/leaving the sole admin is blocked and BA is **never** called; allowed when a second admin remains. Sabotage-verified (drop a guard → red). Guard/lock unit-proven in `lib/workspace-members.test.ts` — 7 cases: `assertRetainsAdmin` (sole/multi/non-admin), `withWorkspaceAdminLock` acquires the `pg_advisory_xact_lock` + reads the count under it + propagates a throw, `resolveWorkspaceMember` isolation. The `member:update`/`member:delete`/`invitation:cancel` RBAC cells are proven in `rbac-matrix.test.ts` (already complete). Remaining gap: no cross-client live sync (out of scope) and the two-account E2E flow (invite→accept→re-role→remove/leave, leave→chooser redirect) is planned, not yet recorded. |
| Notifications (in-app + email + socket) | yes | partial | no | implemented | `lib/notification.test.ts` (17 cases: notifyMentioned 12 + notifyDueDate 5) + `lib/mention.test.ts` (39, @mention extraction/dedup) + `lib/notifications/inbox.test.ts` (8, inbox ordering + unread/pending sums). notifyDueDate unit-proven: creates DUE_DATE notification, sends email, handles email failure gracefully, propagates DB error for claim-rollback (MEDIUM-1). `lib/notification-actions.ts` untested. |
| Due-date reminder scheduler (US-020) | yes | yes | no | implemented | `lib/due-date-reminders.test.ts` (18 cases: milestone selection at boundaries, HIGH-2 archive/delete/completed filter, recipient resolution, SELECT predicate shape). Route integration in `tests/due-date-reminders-route.test.ts` (10: 401 self-guard ×2 + token allow, per-recipient DUE_SOON notify, P2002 dedup skip, MEDIUM-4 error isolation, mixed DUE_SOON+OVERDUE, two-overlapping-ticks idempotency, scheduled-pass where-shape). HIGH-1 stale-reminder cleanup on date change proven in `tests/server-actions/list-card.test.ts` `updateCardDueDateAction` (tx body exercises the real `cardReminder.deleteMany` seam: push-out / clear / set delete the card's reminder rows in the same tx as the date write; unchanged-date resubmit preserves the claim-first dedup). Server driver (setInterval + shutdown handler) manually verified. |
| Real-time sync (socket server/client/emitters) | no | partial | yes | implemented | Wire proven end-to-end, two browser contexts on one board, against real `server.ts` + Postgres. US-009 slice 1 (`e2e/realtime-card-create.spec.ts`): A creates a card → B sees it live. Slice 2 (`e2e/realtime-card-move.spec.ts`): A drags a card across lists (keyboard sensor) → B sees it relocate live; and the **drag-defer invariant** — a remote structural event (archive) is deferred while B is mid-drag, then reconciled on drop (live-rename delivery barrier + socket in-order delivery make it deterministic). Each sabotage-verified (`emitCardCreated`/`emitCardMoved` off; `isDragging` guard removed). US-010 (`e2e/realtime-label-sync.spec.ts`): a label renamed/deleted by one user updates/removes the card-face chip live on another's board — `updateLabelAction`/`deleteLabelAction` fan `card:labels-updated` out per affected card (closes the US-005 limitation); sabotage-verified (fan-out off turns both red) + unit-covered dedupe fix (full `{id,name,color}` snapshot compare). US-011 (`e2e/realtime-card-members.spec.ts`): assign/remove member propagates live to another viewer's open card detail sheet — `assignCardMemberAction`/`removeCardMemberAction` emit `card:members-updated`, store reducer patches `selectedCard` + recomputes the assignable pool (6 unit cases); sabotage-verified (emit off turns it red). US-012 (`e2e/realtime-comment-list-reorder.spec.ts`): a comment posted by one user appears live in another's open card detail sheet (`comment:created`), and a list keyboard-reordered by one user relocates live on another observer's board (`list:moved` — first structural **list** event proven live on an observer; card structural events were proven in US-009). Both emits already wired in `actions.ts`; this is the cross-client proof. Sabotage-verified (both emits off → both observers stale → both red). US-083 W1 (`e2e/realtime-event-proof.spec.ts`): dedicated two-client proof for the six previously-unproven events — `card:updated` (A renames via detail-sheet autosave → B's card face updates live), `list:created` (A adds a list → B sees it), `list:updated` (A renames via inline editor → B sees it), `list:deleted` (A archives via the real archive-list UI → B's column leaves), `notification:new` (A mentions B through the real comment UI; B is workspace-member-only, stays on the board page, bell badge shows exactly `(1 unread)`; a `load` listener fails the test on any reload), `analytics:refresh` (B's dashboard FlowChart "Created" metric appears after A's card create — workspace-room signal → 700ms debounce → router.refresh). Each test: presence barrier + connect-resync settle barrier before A acts; every assertion is in the emit path only, and a masking tripwire (armed per proof window, including the dashboard route in the analytics test) fails the test on any full reload, socket.io disconnect/reconnect, or route re-render POST — so the onConnect fallbacks (header badge resync Server Action, board provider reconnect router.refresh) cannot turn a sabotage run green from persisted DB state. (router.refresh itself is an RSC GET on the wire, so a route POST is never legit delivery; the dashboard route-POST counter also closes the polling-transport reconnect hole, where websocket counters are inert.) Tripwire detection demonstrated: with `notification:new`'s emit removed, a forced mid-window reload previously turned the observer assertion green (masked); with the tripwire armed the same run goes red at the tripwire. All six sabotage-verified (emit body commented in `lib/realtime/server.ts` → that event's observer assertion red; see the US-083 execplan W1 evidence table). One production hook added: `data-testid="flow-chart-created-total"` (the dashboard DOM had no stable locator for the created figure). **US-083 W2 (`e2e/invitation-live-badge.spec.ts`):** three real users — Alice invites registered Bob via the real members dialog → Bob's header badge increments live (`invitation:new` to his own user room, no reload/navigation/inbox-fetch in the assertion path; both observer pages carry the masking tripwire; the connect-resync settle barrier awaits the combined badge-resync action — one Server Action, one route POST); Bob's inbox shows the invitation (DB-truth fetch on open); Carol (connected outsider) sees no badge change (room-scoping denial); Accept clears the badge and Bob becomes a real `workspaceMember` (Postgres-asserted). Bob signs up mixed-case (`BoB-…@E2e.Test`) to prove email normalization end-to-end. Sabotage-verified: `emitInvitationNew` disabled → RED at the Bob live-badge observer assertion (tripwire clean). |
| Automation engine — matcher / conditions / resolver / loop-guard / cycle-check / executor / effects / evaluator (US-066, decision 0022) | yes | yes | no | implemented | `lib/automation/*.test.ts` — 117 cases. `matcher.test.ts` (19): trigger↔config field mapping incl. `card-moved-to-list` destination/source lists. `resolver.test.ts` (13): dynamic-token expansion (`card-assignees`/`card-creator`/`all`/uuid) under workspace-isolation. `loop-guard.test.ts` (18): `ChainTracker` root/child, depth cap, per-chain `(ruleId,cardId)` dedup. `cycle-check.test.ts` (12): action→trigger `producedEvents` mapping + static cycle-warning detection. `executor.test.ts` (32): each action step's tx effect + decision-0030 per-step isolation (all 5 stale-target codes, best-effort continuation, unexpected-abort retained). `effects.test.ts` (7): deferred descriptor → `emit*` mapping. `evaluator.test.ts` (16): matching+logging, loop prevention, error semantics (whole-tx rollback + post-rollback error log), scheduled window gate, `dedupKey` claim-first mode, status derivation (`partially_failed`/`failed`) + `metadata` audit + claim-row finalization. `view.test.ts` (4, US-067 loader query-shape) also cited here. |
| Automation scheduled pass — `due-date-approaching` two-tier dedup (US-066) | yes | yes | no | implemented | `tests/automation-scheduled.test.ts` (8): `maxApproachWindowMinutes`; `evaluateScheduledCard` Tier-1 (`RuleExecutionLog` `@@unique([ruleId, dedupKey])`, key `<cardId>:DUE_SOON`) + Tier-2 (`notify-member` dedup vs the built-in reminder's `CardReminder` milestone). Driven by the existing due-date cron route. |
| Automation rule management Server Actions — admin-only boundary + reads (US-066, decision 0022 §4) | yes | partial | no | implemented | `tests/server-actions/automation-rules.test.ts` (42: 34 rule-management + 8 US-067/075 additions incl. `getBoardAutomationDataAction`, `dryRunRulesAction`). `create`/`update`/`delete`/`toggleEnabled` gated admin-only (`organization:update`); `listRules`/`getRuleExecutionLog` readable by any workspace member; auth/permission/isolation matrix + positive controls, Zod validation. UI (`components/workspace/automation/`) RTL-covered (see the Automation management UI row). **Retention (reconciled US-083 W4):** execution logs SURVIVE rule deletion — migration `20260707021956_automation_logs_survive_rule_deletion` (landed with `dc1fd0a`) denormalized `workspaceId` + `ruleName` onto `RuleExecutionLog`, made `ruleId` nullable, and switched the rule FK from `onDelete: Cascade` to `onDelete: SetNull` (backfilled before NOT NULL); the workspace FK stays `Cascade`, so logs clear only with their workspace. The delete-confirm copy "Past execution-log entries are kept" now matches behavior, and the log panel marks orphaned rows with a `(deleted)` chip (the old "Deleted rule" fallback is gone). End-to-end proof: `e2e/automation-log-retention.spec.ts` (create → trigger → delete rule → reload → log still shows the denormalized rule name; focused re-run 2026-08-02 green, 1 passed — US-083 W4 evidence). |
| Automation management UI (rule list, builder dialog, execution-log panel) | yes | no | no | implemented | `components/workspace/automation/rule-builder-dialog.test.tsx` (US-068, 5 cases, happy-dom): dialog open/close, add/remove action steps, name-required guard (no action call), valid submit → `createRuleAction` payload + `notify("Rule created")` + `router.refresh` + close, server-error surfaced without close (actions + `next/navigation` mocked). Rule-row (`rule-row.test.tsx`, US-068 wave 2, 16 cases) covers enable-toggle + delete-via-AlertDialog + `canManage` gating + last-run states. Rule-list container (`automation-content`, 11) + execution-log panel (16) covered (US-068 wave 2); the automation-management page shell itself remains unrendered. |
| Activity audit log | no | no | no | planned | `lib/activity.ts` untested |
| React client components — RTL foundation + representative set (US-068) | yes | no | partial | implemented | RTL stood up on Vitest as a `components` project (happy-dom, `vitest.workspace.ts`). **402 RTL tests across 38 suites** (28 component suites + 10 app-route suites) in the `components` project. Each mocks its Server Actions + `next/navigation`; behavioral (accessible queries + `user-event`), no snapshots. Boards: `board-filter` (6, store-driven popover), `card-detail-sheet` (39, title autosave-on-blur + guards + read-only + archive (US-069) + queued-save rejection recovery + meta-draft (due/priority/estimate) live reconciliation + comment single-flight — see the Card detail lifecycle safety row), `card-completion-toggle` (10, toggle→action, refresh-on-success-only, `onError`), `card-checklists-section` (24, add/toggle/delete item + progress + gating), `card-labels-section` (13, attach/detach→action + read-only chips), `card-attachments` (12, render/empty/`canEdit` gating — upload file-input not simulated), `create-board-modal` (4, same-tick single-flight + retry after failed/thrown action + reset-on-success), `board-header-rename` (3, draft preservation on in-flight blur / failed save), `board-header-wiring` (7, ArchivedCardsDialog + Share forwarding). Automation: `rule-builder-dialog` (5), `rule-row` (16, enable-toggle + delete-via-AlertDialog + `canManage` + last-run), `automation-content` (11, list/empty/log composition), `execution-log-panel` (30, rows/empty/status/(deleted)-chip), `board-automation-dialog` (7, open + lazy content). Members: `member-row` (15, role-change + remove + last-admin/self gating), `invite-member-dialog` (4, open + email guard + submit→`inviteMemberAction` + error). Notifications: `notification-dropdown` (14, closed/loading/list/empty + mark-read + 3 invitation cases W2). E2E covers async Server Components (RTL cannot render them). Drag-and-drop faces covered by DragDropContext-wrapped suites: `list-card-item` (8), `list-column` (7). Remaining uncovered: board content and dashboard charts — thin in those areas, but the interactive editor/dialog/row surface (comment composer included) is now broadly covered. |
| Safe list lifecycle: archive, restore, guarded permanent delete (US-074, decision 0026) | yes | yes | yes (unit) | implemented | **Slices A & B (stable):** `archiveListAction` / `deleteListAction` soft-archives list, `restoreListAction` / `getArchivedLists` / `getArchivedListWithBoard`, UI restore drawer. Proven across list-lifecycle, board-store, and component tests. **Slice B2 (stable):** Archived-list boundary hardening — 19 parsed-layered tests prove central resolver guards reject mutations on cards under archived lists. **Slice C (guarded permanent delete) — production implementation:** `permanentlyDeleteListAction` with admin-only `organization:["update"]` gate, exact case-sensitive title confirmation, Cloudinary-attachment guard under FOR UPDATE lock (decision 0029), live-cards block unless explicit `force`, transaction-body CARD_DELETED history events, conditional `deleteMany` with count=0 rollback, list:deleted + analytics:refresh emit. **In-flight upload race closed:** `uploadAttachmentAction` and `setCardCoverAction` acquire FOR UPDATE lock on parent List and revalidate `archivedAt IS NULL` before inserting attachment under transaction client (`createAttachment`, `createActivityEntry`, `updateCardCover` accept `Prisma.TransactionClient`). On revalidation failure, the just-uploaded Cloudinary asset is compensated via `cloudinary.uploader.destroy` with exact publicId + resourceType. **`permanentlyDeleteListAction`** moved Cloudinary check inside the FOR UPDATE transaction. `verifySession()` is the first operation (early unauthenticated rejection + userId capture), reused inside tx. **Proof classification:** **Auth/permission/isolation (A1/A2/A3) — denial tests (not sabotage):** 43 integration tests in `tests/server-actions/list-lifecycle.test.ts` covering auth, RBAC (A2 viewer/editor/admin), isolation (A3 cross-workspace), precondition guards, Cloudinary gate, live-cards, history events, concurrent restore, emit, double-purge, force guards, FOR UPDATE call-shape + revalidation edge cases, RBAC role-map (`canPermanentDelete` ↔ `organization:update` + `getBoardPagePermissionsForRole` direct test). **Sabotage tests (load-bearing guard removal turns red):** admin gate removal (A2/A3 for editor); force-does-not-bypass (auth/Cloudinary/title/archived-list) — 4 tests. FOR UPDATE lock removal test (lock call-shape assertion). **PostgreSQL mechanism proof (PG interleaving, not call-shape):** `tests/db-index-proof.test.ts` — 6 tests (3 PG lock_timeout interleavings — producer lock blocks archiver; archiver lock_timeout proves producer hold; purge lock freezes producer — plus migration-execution/count-check/sandbox-harness cases). **Producer lock + compensation proof (action-level):** `tests/server-actions/list-card.test.ts` + `tests/server-actions/card-priority-cover.test.ts` — 4 tests (2 revalidation-failure + compensation, 2 success-no-compensate). Cloudinary module is vi-mocked. **Role-map proof (direct, not via page.tsx):** `getBoardPagePermissionsForRole` unit test in `list-lifecycle.test.ts`. **BoardHeader forwarding (RTL, mocked ArchivedCardsDialog captures prop):** `components/boards/board-header-wiring.test.tsx`. **ArchivedCardsDialog component proof (22 RTL):** `components/boards/archived-cards-dialog.test.tsx` — admin wiring (canPermanentDelete true/false), exact-title confirmation, `liveCardCount` drives force checkbox, Cloudinary block with no submit path, dialog stays open during async (pending/error/retry/duplicate). **page.tsx Server Component — NOT tested (async RSC, covered by type-check + code review).** E2E **— NOT IMPLEMENTED (known gap).** UI no longer claims `cloudinaryBlocked` passed incorrectly; `take: 1000` removed (bounded set-dedup). **Decision 0029** updated for FOR UPDATE lock protocol + producer compensation + residual orphan risk. Full focused suite green (202 tests: 43 list-lifecycle + 133 list-card/card-priority (116+17) + 22 archived-cards-dialog + 4 wiring, incl. 1 sabotage). |
| Automation rule failure isolation & stale target handling (US-075, decision 0030) | yes | yes | no | implemented | **Executor per-step isolation:** each action step runs in its own try/catch inside the trigger tx. A structured stale-target `RuleExecutionError` (codes `TARGET_LIST_NOT_FOUND` / `TARGET_LIST_ARCHIVED` / `TARGET_LIST_FOREIGN_WORKSPACE` / `MEMBER_NOT_IN_WORKSPACE` / `LABEL_NOT_FOUND`) is audited per-step and the NEXT independent step still runs (best-effort). Overall status: `partially_failed` (≥1 step failed + ≥1 succeeded), `failed` (all failed), `success` otherwise. **Unexpected (non-`RuleExecutionError`) errors still abort** + post-rollback `error` row (class 2 retained); the 5 board-action catch sites now surface "hit an unexpected error" — the "no changes were applied" message is retired for the isolated class. Per-step audit `{stepIndex, actionType, status, code, targetId, message}` lands in `RuleExecutionLog.metadata` (no schema migration). Claim-first dedup (invariant #6): a partially-succeeded rule KEEPS its claim — the row is finalized in place via update; only fully-aborted (unexpected) attempts stay retryable. Deferred effects + cascades fire only from succeeded steps. **Proof — unit (117 `lib/automation/*` — see the US-066 row):** executor isolation for every code incl. a 3-step rule whose stale middle step leaves siblings committed; unexpected-error abort; evaluator status derivation + metadata shape + claim finalization. **Proof — integration (`tests/server-actions/automation-failure-isolation.test.ts`, 4):** real action→evaluator→executor — createCard with deleted-list middle step returns `success:true` + `partially_failed` row with `TARGET_LIST_NOT_FOUND` + stale id in metadata; deleted-label target → `LABEL_NOT_FOUND`; moveCard with archived target → user's move commits + `failed` row with `TARGET_LIST_ARCHIVED`; unexpected error → action fails with the new message + post-rollback `error` row. **Mutation-checked:** (a) removing the per-step try/catch re-breaks the primary mutation (red); (b) removing the `partially_failed` status assignment breaks the audit assertion (red). **Absorbed US-074 minor:** `actionTargetsInWorkspace` rejects ARCHIVED list targets at rule save time (UX guard). Decision 0030 recorded. UI audit panel surfacing of `metadata` is optional/future; data queryable via `getRuleExecutionLogAction`/DB. |
| First-party product usage measurement baseline (US-076, decision 0027) | no | planned | planned | planned | No external analytics, no PII by default. Gated by Decision 0027. Implementation unstarted. |
| Today / My Work read-only cross-board view (US-077 → absorbed by US-083 W6) | no | no | no | retired | Full ACs remain in the retained US-077 packet; US-083 W6 incorporates them by exact reference (self-audit mapping in the US-083 packet). No implementation exists under US-077; any proof will be recorded under the US-083 row. |
| Global quick capture using standard Card creation (US-078 → absorbed by US-083 W7) | no | no | no | retired | Full ACs remain in the retained US-078 packet; US-083 W7 incorporates them by exact reference (self-audit mapping in the US-083 packet). No implementation exists under US-078; any proof will be recorded under the US-083 row. |
| Demo-ready daily work loop (US-083, decision 0031) | implemented (W1, W2, W3, W4, W6, W7) | implemented (W2, W6, W7) | implemented (W1, W2, W3, W4, W6, W7, W8 + demo rehearsal) | implemented (merged via PR #94 on 2026-08-02) | Single high-risk story combining the owner's delivery Stage 1 (W1–W5: cross-client proof for `card:updated`/`list:created`/`list:updated`/`list:deleted`/`notification:new`/`analytics:refresh`, live invitation badge, repeatable demo seed/reset, automation retention + tracker reconciliation) and Stage 2 (W6 Today / My Work, W7 global quick capture via `createCardAction` + `C`/`Cmd/Ctrl+K`, W8 bounded undo snackbar for archive card/list only via real restore actions, incl. parent-list-archived race guard). **W1 landed (committed 937e75f):** `e2e/realtime-event-proof.spec.ts` — six isolated two-client tests, one per event, each sabotage-verified (emit removed → observer red; six recorded RED runs in the execplan), with a masking tripwire that fails any proof window on reload/socket-reconnect/route-re-render (reconnect-masking gap demonstrated and closed, execplan evidence table + run log). **W2 landed (committed 3f238be):** live invitation arrival — typed `invitation:new` user-room event (`emitInvitationNew`, invitee-only; payload `{ invitationId }`), emitted best-effort from `inviteMemberAction` for an already-registered invitee (normalized-email resolution, BA casing verified at source); header badge increments live, connect resync reads BOTH badge halves from the DB in one action (`getInboxBadgeCountsAction` replaces `getUnreadNotificationCountAction`); inbox unchanged (DB-truth on open). Unit/component proofs: `lib/realtime/server.test.ts` (4), `lib/invitation.test.ts` (3), `workspace.test.ts` (+3), `authenticated-header-actions.test.tsx` (4). E2E: `e2e/invitation-live-badge.spec.ts` — three real users, badge increments live for Bob, inbox shows it, Carol unaffected (denial), accept clears badge + Postgres membership; mixed-case Bob signup proves BA email normalization end-to-end; masking tripwire + dropdown-open ordering guard; sabotage-verified (emit off → RED at the live-badge observer assertion). Full gates: `npm test` 1221 green, full E2E 18 green (incl. all six W1 proofs — the combined resync preserves the single-POST connect barrier). **W3 landed (commits 827f222, d127762):** repeatable `demo:seed`/`demo:reset` fixture + manifest + enforced stale-server restart protocol (`docs/DEMO_RUNBOOK.md`). **W4 landed (committed dc1fd0a):** automation execution-log retention reconciled — the rule-management row's cascade "Known discrepancy" is resolved (migration `20260707021956_automation_logs_survive_rule_deletion`, committed `dc1fd0a`); US-066 packet + `docs/product/automation.md` fixed (`errorDetails` removed — actual fields `error` / `metadata.steps`; one row per evaluation, not per action step; no retention/prune window — logs clear only on workspace cascade; logs survive rule deletion via SetNull + denormalized `ruleName`). Focused `e2e/automation-log-retention.spec.ts` re-run green (1 passed, 2026-08-02). **W5 landed (committed b1280f8):** tracker/harness truth reconciled — `docs/stories/backlog.md` + IN-04 US-083 rows moved to in-progress (W1–W5 landed — W4/W5 uncommitted on the feature branch, W6–W8 pending), US-075 row marked implemented (PR #92); harness story rows updated (US-083 `in_progress` with Stage 1 evidence, US-066/075 `implemented` with evidence, US-075 `story verify` pass recorded); durable decision rows 0029/0030 added (docs existed without harness rows); `harness-cli audit` + `query matrix` evidence recorded in the packet's validation.md. **W6 landed (committed dcc481b, 2026-08-02):** Today / My Work — cross-workspace personal read model (`lib/today-query.ts` `getPersonalWorkCards(userId)`): workspace scope derived server-side from the caller's `WorkspaceMember` rows (`workspaceId: { in: <memberships> }`, never client-supplied), live-card contract (card `archivedAt`/`deletedAt`/`completedAt` null + list `archivedAt` null + board `archivedAt` null + `members: { some: { userId } }`), one membership query + one bounded card query (no N+1), serializable ISO-string model. Pure bucket helpers (`lib/today.ts`): Overdue diff<0 / Due Today diff=0 / Due This Week 1..7 / Later ≥8 or none — calendar-day diff in viewer-local time (DST-proof day-number arithmetic, injected `now`). `/today` = async RSC (auth + query) + client boundary (`components/today/today-view.tsx`, `now` injectable) with real deep links `/boards/{boardId}?cardId={cardId}` (no in-place sheet), workspace · board · list context, non-color-only due/priority chips (DESIGN.md meta-chip voice), accessible empty states (zero memberships / nothing assigned). Discoverable Today entry in the authenticated global chrome (`components/today/today-nav-link.tsx`, `aria-current` active state). Proof: `lib/today.test.ts` (16 — boundary/partition/DST/ordering, en-US-pinned labels), `tests/server-actions/today.test.ts` (7 — membership derivation, isolation `in` clause, live-card where shape, bounded select, serializability, zero-membership empty model, single-query no-N+1), `components/today/today-view.test.tsx` (11 — sections/counts, deep links, context, due/priority a11y chips, empty states, completed excluded, SSR-deterministic hydration boundary), `app/(authenticated)/(dashboard)/today/page.test.tsx` (3 — RSC wiring: verifySession → `getPersonalWorkCards(userId)` → TodayView props + metadata), `today-nav-link.test.tsx` (2), `app/(authenticated)/layout.test.tsx` (1 — chrome presence). Focused run 40/40 green, `tsc --noEmit` clean, changed-file ESLint clean, `git diff --check` clean. Time-dependent grouping/labels render only after a client-mounted boundary (deterministic SSR skeleton + `role="status"`, clock captured at first client render, no `suppressHydrationWarning`); date labels pinned to `en-US` (English UI); dead `startOfDayLocal` removed; unbounded personal read model (no pagination) recorded as a residual follow-up — no silent cap, AC1 forbids hiding cards. `e2e/today.spec.ts` 4/4 green (2026-08-02 shared-server lock run — one arrange-defect repair: `createWorkspace` gained the user-menu path for members with workspaces; RED mechanism + run log in the US-083 validation.md); US-077 packet AC1 amended to the cross-workspace interpretation; product section-name drift reconciled (`boards-and-cards.md` "Today/Upcoming/Unscheduled" → locked "Due Today/Due This Week/Later"; `overview.md` cross-board → cross-workspace). **W7 landed (committed in this W7 commit, focused-test checkpoint):** global quick capture — authenticated chrome button + global shortcuts (bare `C` = reliable demo path; `Cmd/Ctrl+K` implemented/tested with the browser-chrome reservation caveat — portability not claimed); dialog opens immediately, scope loads lazily on first open via the one new read-only authenticated action `getQuickCaptureOptionsAction` (`lib/quick-capture-options.ts`: editor/admin-creatable memberships only, active boards/lists, deterministic membership/board order, exactly four bounded queries, no N+1); defaults route → saved (localStorage, per-field validity) → first creatable, list = saved-valid-or-left-most, list-less board stays selected with honest disabled submit; `createCardSchema`/`createCardAction` extended backward-compatibly (description/dueDate/priority persist in the SAME transaction — no chained updates, no wrapper mutation; auth/permission-obfuscation/position/history/automation/revalidation/emits preserved); `card:created` payload + board-store reducer carry dueDate/priority fidelity for observers (optional fields, null fallback — existing consumers untouched). Proof: `lib/quick-capture.test.ts` (33 — defaults/fallbacks, per-field saved validity, list-less board, shortcut guard matrix incl. copy/Shift+C/repeat/IME/overlay guards, storage contract), `tests/server-actions/quick-capture.test.ts` (18 — options action auth/membership-role isolation, active-only scoping, determinism, four-query no-N+1 shape; createCard A1/A2/A3/archived-board regression, one-create optional persistence, empty-string mapping, invalid priority/date boundary, position gap, automation evaluation inside the tx, pinned CARD_CREATED history payload, emit payload fidelity, revalidate, bare-title backward compat, restoreCardAction emit fidelity), `components/quick-capture/quick-capture.test.tsx` (23 — immediate open, lazy first-open-only load, mid-flight-close refetch with stale-request discrimination (late resolve/reject never overwrite the new request), all default paths, list reset on board change, required trimmed title, one-FormData optional payload, success toast/deep link + persistence, inline error, empty-state, 375px-safe classes/a11y labels), `components/quick-capture/quick-capture-shortcuts.test.tsx` (13 — C/Ctrl+K/Meta+K handled with preventDefault exactly-once; input/textarea/select/contenteditable, copy, Shift+C, repeat, IME, open-dialog, open-overlay guards with preventDefault zero), header chrome + board-store fidelity + absent-field null-fallback cases. Correction pass (reviewer + proof-auditor findings, no commit): stuck-ref lifecycle fix + controlled-from-first-mount Selects (zero uncontrolled↔controlled warnings), restoreCardAction emit fidelity, non-vacuous e2e route-default arrangement, RED row-2 narrative corrected (partial production state, not tests-only/no "rejects"). Focused W7 gate 146/146 green (final post-correction run — baseline was already 141, not 140; per-file 33+18+23+13+5+54); affected-area regression 229/229 green (list-card, card-priority-cover, automation-failure-isolation, checklist, card-history, board-store, board-store-provider, layout; board-store counted in both runs by design); `tsc --noEmit`, changed-file ESLint, `git diff --check` clean. `e2e/quick-capture.spec.ts` **5/5 GREEN on the final shared-server run (2026-08-02, ≈1.3–1.4m)** — /today C invocation + deterministic defaults, optional-field persistence (description/due date/priority) + deep-link toast click-through, saved-destination fallback, route default + C input-focus guard (US-043 two-Escape semantics), two-client live appearance with W1 presence barrier + connect-resync settle + masking tripwire (priority chip visible on the observer board), Cmd/Ctrl+K opener (headless; browser-chrome reservation caveat stands). Post-checkpoint fixes landed before the run: hydration-readiness marker (`data-shortcuts-ready`, listener-owned — `openCapture` waits before the first keydown) + US-043 two-Escape arrangement. Full-suite gate at the stable checkpoint (2026-08-02, after the readiness-marker edit): `npm test` 1351 green (85 files), `tsc --noEmit` / changed-file ESLint / `git diff --check` clean. Shortcut convention recorded in DESIGN.md (first global shortcut owner). **W8 landed (2026-08-02, committed in this W8 commit):** bounded undo — one narrow undo host/state machine (`components/undo/undo-snackbar.tsx` `UndoHost`, mounted in the board page inside `BoardStoreProvider`, survives archived-entity unmount + realtime/RSC updates) with a pure eligibility map + reducer (`lib/undo.ts`: exactly card/list archive; every non-goal ✗) and exactly two offer seams (shared `ArchiveCardDialog`, `ListColumn` archive menu — ids from the call sites, never archive result types). Undo calls the real `restoreCardAction`/`restoreListAction` (pessimistic; thrown actions caught; in-flight "Restoring…" disabled; latest-offer-wins; manual dismiss; navigation dismissal; 8s offer TTL; polite `role="status"` success, assertive `role="alert"` failure with the action's own message; no focus steal). Parent-list-archived race: sequential discriminator (no-leak, permission-gated) + in-tx FOR UPDATE revalidation (see the card archive/restore row). Proof: `lib/undo.test.ts` (16, incl. the generation-tagged stale-outcome race cases), `tests/server-actions/undo-restore.test.ts` (13), `tests/db-undo-race-proof.test.ts` (3 real-Postgres interleaving; guard-off sabotage reddens tests 1 AND 3 — recorded), `components/undo/undo-snackbar.test.tsx` (14 RTL, incl. the A-in-flight/B-offered race), `archive-card-dialog.test.tsx` (2 RTL), `list-column.test.tsx` (+1 RTL seam), resolver discrimination (3); correction-pass re-runs 47 + 23 + 137 green (lib/undo+card+undo-restore / RTL / db-proof+quick-capture+list-card), `tsc --noEmit`/ESLint/`git diff --check` clean; action-level + DB-level guard-removal sabotage both RED (recorded); the pre-correction checkpoint was `npm test` 1395 green; the correction pass changed production (generation-tagged outcomes) and re-verified the affected area (47 + 23 + 137 = 207 tests) — final full-suite gate: `npm test` 1400 green (90 files). Transient Feedback convention added to DESIGN.md; product doc + decision 0031 updated. `e2e/undo-snackbar.spec.ts` **5/5 GREEN (2026-08-02, Root-granted shared-server lock, 1.5m)** — card undo in place with reload/socket tripwire, list undo with its own tripwire, two-client race with survive-window tripwire + truthful "Restore the list first." + no invisible restore (DB-asserted), member-removal/label-deletion absence with gated overlay closes (non-vacuous), permanent-delete absence + row-gone DB assertion. The gate caught one product defect (a `"use server"` const export that broke the authenticated module graph — de-exported, module-private) and three test-defect corrections (alert locator vs Next's route announcer, two-context sign-up, overlay-click closes + settle) — run log in the US-083 validation.md W8 E2E section; no assertion/tripwire/overlay gate weakened. **Final close (2026-08-02, branch-local):** W3 exit gate executed for real — the two demo users were provisioned through the REAL sign-up + Mailpit verification flow and the seed→reset→seed round trip reproduced 2 boards / 5 lists / 7 cards with identical titles, roles, and relative due-date offsets (all 14 entity ids + workspace id differed per run; manifests + comparison in the validation.md W3 section); the continuous locked demo path runs from the seeded fixture in one sitting (`e2e/demo-rehearsal.spec.ts` 1/1 green — /today buckets, quick capture, two-client realtime, card+list archive→Undo, live invitation badge, fixture-preservation proof); the rehearsal caught and the final-close pass fixed two real fixture defects (nondeterministic board order — pinned 1ms-offset board `createdAt`; UUID workspace id rejected by the app's workspaceId schemas — fixture now seeds a 32-char id) plus a test-side archive ambiguity (sheet-scoped, non-vacuous assertions); W3's unit-proof cell is covered by `tests/demo-fixture.test.ts` (8 pre-existing safety cases + 4 contract cases added by the correction pass: 32-char workspace id, pinned board createdAt order, logical shape, write-path persistence; sabotage-revert RED 3 failed / 9 passed → GREEN 12/12); 375px platform proof green (`e2e/platform-375.spec.ts` 3/3 — no horizontal overflow on /today, Quick Capture, and the undo snackbar; C shortcut inert while typing); the undo spec's non-goal sheet-reopen gained a router.replace-commit wait (observed flake, focused 3/3 green); combined US-083 E2E gate 25/25 green, full E2E suite 36/36 green — re-proven post-self-provisioning correction on the final tree (10.2m, count unchanged; correction chain 35/1 → 35/1 → 36/36 with two US-009 baseline specs gaining the W1 presence barrier, validation.md combined-gate section), `npm test` 1404 green (90 files), `tsc --noEmit` / changed-file ESLint / `git diff --check` clean; harness row updated to implemented with unit/integration/e2e/platform = 1, `story verify` pass, completed close trace, audit/matrix before/after evidence in the validation.md final-close section. |
| Per-board Capture / Triage using first-class Cards (US-079) | no | planned | planned | planned | Default landing list for quick-captured cards. Escalation required if schema changes. Implementation unstarted. |
| Automation trigger expansion for attribute changes (US-080) | no | planned | planned | planned | Triggers for due date, estimate, priority, stale-in-capture. No webhooks/SLAs. Implementation unstarted. |
| Card templates standalone vertical slice (US-081) | no | planned | planned | planned | Reusable card templates. Data model unresolved; decision gate inside packet. Implementation unstarted. |
| Recurring cards built on accepted templates (US-082) | no | planned | planned | planned | Scheduled card creation from templates. Scheduler/dedup unresolved; depends on US-081. Implementation unstarted. |

**Presentation-conformance rows (US-048/049/050/051/052/053/054/055) are closeout-only manual-QA records** — their evidence is manual browser QA, not automated proof. Any future automated test on these surfaces must NOT pin retired hex/px/oklch/AA values: they change with token updates.

## Coverage Snapshot

- **Well-proven:** the pure/computational core — card-history event derivation,
  DnD position math, Zustand reconciliation, analytics computation + export.
- **Now proven (US-006):** the Server Action *security boundary* — auth,
  permission gate, and workspace isolation on every mutating action (+ analytics
  read isolation). What remains untested on those actions is their *business
  logic* (position math, lifecycle transitions, payload shaping).
- **Now proven (US-007):** the full RBAC role × action allow/deny matrix against
  the real `admin`/`editor`/`viewer` roles, the board-page UI map's agreement
  with it, and the US-006 harness copy's fidelity to it.
- **Now proven (US-066):** the automation rules engine end to end below the UI —
  trigger matching, dynamic-target resolution, four-layer loop prevention,
  action execution, deferred-effect emission, the evaluator's error/rollback and
  dedup semantics, the scheduled two-tier dedup, and the admin-only Server Action
  boundary (167 cases: 117 `lib/automation/*` + 8 `tests/automation-scheduled` +
  `tests/server-actions/automation-rules`). The rule builder + rule-row +
  rule-list container + execution-log panel now have RTL coverage (US-068); the
  automation-management page shell remains unrendered.
- **Now proven (US-075, decision 0030):** automation failure isolation — a stale
  rule target (deleted/archived list, departed member, deleted label) NEVER rolls
  back the user's primary card mutation; the step is audited with a structured
  code + target id in `RuleExecutionLog.metadata`, remaining independent steps
  still run, and overall status is `success`/`partially_failed`/`failed`.
  Unexpected errors retain abort + post-rollback logging; claim-first dedup
  survives partial success. Proven by `lib/automation/*.test.ts` (117) +
  `tests/server-actions/automation-failure-isolation.test.ts` (4), both
  mutation-checked (isolation removed → primary mutation fails again;
  `partially_failed` assignment removed → audit assertion fails).
- **Largest gaps (highest risk first):** Server Action business logic, real-time
  emitters, notifications, and most React components (RTL now exists with a
  representative set — US-068 — but the bulk of the component tree is still
  uncovered).

## Evidence Rules

- Unit proof covers pure domain/application rules (`lib/dnd`, `lib/card-history`).
- Integration proof covers Server Actions, DB writes, and provider behavior
  (use Prisma mocks via `vi.mock("@/lib/prisma")` as the existing tests do).
- E2E proof covers user-visible browser flows via **Playwright** (`e2e/**`,
  `npm run test:e2e`), introduced in US-009. Proven: two-client realtime
  card-create (slice 1), card-move across lists, the drag-aware deferral
  invariant (slice 2), label rename/delete propagation (US-010), card-member
  assign/remove propagation (US-011), and comment propagation + list reorder
  (US-012). Card and list drags are driven with the keyboard
  sensor — `@hello-pangea/dnd` ignores synthetic pointer/CDP drags.
- A story can ship without every proof column if its packet explains why.
- New Server Actions are the priority for new tests: they enforce auth,
  permission, workspace isolation, and Zod validation — the security-critical
  boundary.
- **CI gate (US-008):** `.github/workflows/ci.yml` runs `lint → tsc --noEmit →
  npm test` on every PR/push into `dev`/`main`, so the proofs in this matrix run
  automatically. The gate is advisory until added to branch protection as a
  required status check. E2E server-reuse policy unit-proven
  (`tests/e2e-server-policy.test.ts`, 2: fresh server by default for local/CI
  runs, explicit local opt-in only).
