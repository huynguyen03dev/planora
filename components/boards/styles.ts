// Workspace identity badge surface (US-053): a neutral chip, NOT a chromatic
// accent. The badge repeats per workspace across the sidebar and boards
// overview, so a colored fill would make a second accent ambient in the chrome
// (DESIGN.md §183/§386 — brand blue stays scarce; no second chromatic accent).
// Initials sit on the neutral secondary surface with its guaranteed-contrast
// foreground. Shared by workspace-item, workspace-section, workspace-boards-view.
export const workspaceBadgeSurface = "bg-secondary text-secondary-foreground"
